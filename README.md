# PicOrgFTP-SQL
Picture orgraniser with ability to send to FTP and SQL

## Editing existing entries

The interface now provides a **Popraw dane** button. It allows loading an
entry by its EAN, reviewing current data and adjusting the name, type, model
or colours. During submission the application moves files from the original
directory to the new location and updates local records as well as the
database.
